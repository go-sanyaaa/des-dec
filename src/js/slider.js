import 'owl.carousel/dist/assets/owl.carousel.min.css'
import 'owl.carousel'

$(document).ready(function(){
    $('.owl-carousel').owlCarousel({
        items: 2,
        margin: 20,
        autoWidth: true
    });
});